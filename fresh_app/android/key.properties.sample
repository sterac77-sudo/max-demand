# Copy this file to fresh_app/android/key.properties and fill in values.
# Keep your real key.properties OUT of git (it's ignored in .gitignore).

storePassword=your-keystore-password
keyPassword=your-key-password
keyAlias=upload
# Path is resolved RELATIVE TO the android/ directory (rootProject in Gradle).
# If your JKS is at fresh_app/android/upload-keystore.jks, use:
storeFile=upload-keystore.jks
